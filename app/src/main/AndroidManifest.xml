<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs50final.com.cs50final">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!--Always keep android:theme="@style/AppTheme" -->

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ABCMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Homepage">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ABCMain" />
        </activity>
        <activity android:name=".Literacy">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LiteracyHome" />
        </activity>
        <activity android:name=".LiteracyHome">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homepage" />
        </activity>
        <activity android:name=".Math">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homepage" />
        </activity>
        <activity android:name=".Flashcard_Lit">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LiteracyHome" />
        </activity>
    </application>

</manifest>